{
  "name": "my-library",
  "license": "MIT",
  "version": "0.0.1",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "esbuild src/index.js --platform=node --bundle --outdir=dist --watch",
    "build": "esbuild src/index.js --platform=node --bundle --minify --outdir=dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "esbuild": "^0.17.2",
    "vitest": "^0.27.2"
  }
}
